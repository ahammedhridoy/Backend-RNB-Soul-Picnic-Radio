import { Button } from "@mui/material";
import React from "react";

const LoadMoreBtn = () => {
  return (
    <div>
      <Button variant="contained" className="bg-[var(--gray-color)]">
        Load More
      </Button>
    </div>
  );
};

export default LoadMoreBtn;
